interface:
  display_name: "CB Connect Browser QA"
  short_description: "Run safe CB Connect browser and CLI QA workflows"
  default_prompt: "Use $cb-connect-browser-qa for CB Connect QA, especially authenticated Clerk/Convex and local test-environment qualification before pushing."

policy:
  allow_implicit_invocation: true
